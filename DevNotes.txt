Feb-17: WOrking on index.js line 316: "// ⬇⬇⬇ tie this one below to the auto fill"

Tryoing to extract using below function"

'''
const select = document.getElementById('planSelect');
select.addEventListener('change', () => {
  const opt = select.selectedOptions[0];     // the selected <option>
  const plan = select.value;                 // "basic" | "standard" | "premium"
  const price = opt.dataset.price;           // e.g., "19.99"
  const billing = opt.dataset.billing;       // e.g., "monthly"

  console.log({ plan, price, billing });
});
``
